apiVersion: v1
kind: Pod
metadata:
  name: two-containers-pod
  labels:
    app: my-app
spec:
  containers:
    - name: writer
      image: nginx
      ports:
        - containerPort: 80
    - name: reader
      image: nginx
      ports:
        - containerPort: 8080
